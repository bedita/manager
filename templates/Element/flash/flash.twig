{% set level = level|default('info') %}

<flash-message :options="{{ config('Flash')|json_encode }}" level="{{ level }}" message="{{ message }}" :params="{{ params|json_encode }}" view-name="{{ _view.name }}" :user-roles="{{ user.roles|json_encode }}"></flash-message>
