<flash-message
    level="{{ level|default('info') }}"
    message="{{ message }}"
    view-name="{{ _view.name }}"
    :options="{{ config('Flash')|json_encode }}"
    :params="{{ params|default({})|json_encode }}"
    :user-roles="{{ user.roles|default([])|json_encode }}">
</flash-message>
