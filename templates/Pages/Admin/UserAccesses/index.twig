{{ element('Admin/sidebar') }}

{% do _view.assign('title', __('Administration') ~ ' ' ~ __('User Accesses')) %}
{% do _view.assign('bodyViewClass',  'view-module view-admin') %}

<user-accesses></user-accesses>
