{# Intentionally left blank: you may override this element in a plugin to create a custom view block

  To do so use a configuration like this in config/app.php:

    'Elements' => [
        'my_models' => [
            // element path => plugin to use
            'Form/custom_right' => 'MyPlugin.Form/my_custom_template',
        ],
    ],

 This way for `my_models` object type a template file `MyPlugin.Form/my_custom_template` is loaded instead of this blank file #}
